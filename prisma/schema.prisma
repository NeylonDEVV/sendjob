datasource db {
  provider = "postgresql"
  url      = env("DATABASE_PRISMA_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Empresa {
  id    Int    @id @default(autoincrement())
  email String @unique
}
